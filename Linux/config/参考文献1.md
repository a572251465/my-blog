```conf
server {
    listen       80;
    server_name  www.lihh.com;

    #charset koi8-r;

    #access_log  logs/host.access.log  main;

    location / {
        return 200 "lihh";
    }

    error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   html;
    }
}
```

```conf
server {
    listen       80;
    server_name  www.chendd.com;

    #charset koi8-r;

    #access_log  logs/host.access.log  main;

    location / {
        return 200 "chendd";
    }

    error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   html;
    }
}
```

- 上述配置中字段`server_name` 设置不同的名称可以通过不同的域名进行访问
